theme: jekyll-theme-minimal
plugins:
  - jekyll-seo-tag
title: The Reading Room BKK — Audio Enhancement
description: Audio quality improvement for 429 YouTube recordings (341 hours, 2010–2019) from The Reading Room Bangkok. 9 ML/DSP pipelines benchmarked across 161 events.
url: https://phoneee.github.io
baseurl: /readingroom-audio

# Default OG image for pages that don't specify their own
defaults:
  - scope:
      path: ""
    values:
      image: /readingroom-audio/og-image.png
